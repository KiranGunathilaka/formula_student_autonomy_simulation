# falcon_drivers

Placeholder for ZED and LiDAR driver configuration and wrappers.

- **ZED**: Add ZED 1 camera configs and ROS 2 wrappers here. Vendor drivers (e.g. `zed-ros2-wrapper`) should be added as external dependencies or submodules.
- **LiDAR**: Add model-specific 2D LiDAR driver packages here. Recommended approach: vendor drivers as external deps/submodules; keep launch/config in this package.

This should go on to prod container 
